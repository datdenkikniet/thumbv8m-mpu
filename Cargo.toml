[package]
name = "cortexm33-mpu"
version = "0.1.0"
edition = "2024"

[dependencies]
arbitrary-int = "2.1.1"
bitbybit = "2.0.0"
cortex-m = "0.7.7"
defmt = { version = "1.1.0", optional = true }

[features]
default = [ "defmt" ]
defmt = ["dep:defmt"]
